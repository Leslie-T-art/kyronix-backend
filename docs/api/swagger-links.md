# Swagger Links

Current service Swagger UI endpoints in the workspace:

- Auth Service: `http://localhost:8081/swagger-ui.html`
- OLTS Service: `http://localhost:8082/swagger-ui.html`

OpenAPI JSON endpoints:

- Auth Service: `http://localhost:8081/v3/api-docs`
- OLTS Service: `http://localhost:8082/v3/api-docs`

Notes:

- These URLs are available when the corresponding service is running locally.
- In production, Swagger should remain disabled or access-restricted.
