server:
  port: 8090
spring:
  application:
    name: self-assessment-service
management:
  endpoints:
    web:
      exposure:
        include: health,info
kyronic:
  audit:
    service-url: http://localhost:8083
