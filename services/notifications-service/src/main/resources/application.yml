server:
  port: 8084
spring:
  application:
    name: notifications-service
management:
  endpoints:
    web:
      exposure:
        include: health,info
kyronic:
  audit:
    service-url: http://localhost:8083
