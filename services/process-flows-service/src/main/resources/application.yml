server:
  port: 8089
spring:
  application:
    name: process-flows-service
  datasource:
    url: jdbc:postgresql://localhost:5433/kyronic_platform
    username: kyronic
    password: kyronic
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: process_flows_service
  flyway:
    enabled: true
    default-schema: process_flows_service
    schemas: process_flows_service
    table: process_flows_service_schema_history
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
security:
  jwt:
    secret: kyronic-local-jwt-signing-secret-2026
springdoc:
  swagger-ui:
    path: /swagger-ui.html
kyronic:
  audit:
    service-url: http://localhost:8083
